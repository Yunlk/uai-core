# AGENTS.md

给在此仓库工作的 AI 编码代理的约定与边界。**动手前先读这份**。

---

## 一、这个仓库是什么

U 校园（Unipus）在线学习平台的接口客户端，Rust 实现：

- `uai-core`（lib）：接口层 + 题型层，可单独 import、单独测试
- `uai`（bin）：CLI 薄壳，只负责参数解析与输出，**不含业务逻辑**

采用**非商业使用许可**（见 `LICENSE`）：允许任意改写与再分发，**禁止商业使用**，
衍生作品须采用同等条款。

## 二、八条硬规则（违反即视为改坏）

1. **绝不写入任何真实账号数据。** 代码、测试、文档、示例里一律用占位符
   （如 `1111111111111111111`、`course-v2:example…`、`20000000001`）。
   真实 `classId` / `courseId` / `courseResourceId` / 实例 ID / `open_id` /
   `school` / 姓名都**不得**出现在仓库中。
2. **绝不提交凭据与会话缓存。** 凭据只从环境变量 `UAI_USER` / `UAI_PASS` 读；
   会话缓存只写系统临时目录，且**永不打印令牌本身**。
3. **写操作只允许集中在既定模块里。** 新增写接口前先想清楚是否真的必要；
   其余代码必须保持只读。审阅者应能一眼看出"哪些代码会改服务端"。
4. **不伪造时间戳、分数与身份。** 客户端只提交平台要求的载荷，
   服务端的账以服务端为准；不要试图构造"看起来像真进度"的数据。
5. **不实现考试 / 测验类任务。** 该类任务通常只有一次机会且不可回滚。
   如需研究，只能写**只读探针**（放在 `examples/`），不得有写入路径。
6. **尊重速率限制。** 批量提交每组至少间隔 20 秒；被限流后必须**真正等待**
   （当前退避 45 秒），退避为 0 的"猛蹬"只会烧光配额。
7. **两套 token 不得混用。** 门户 JWT 与自铸的接口 token 是不同体系，
   混用会得到难以定位的空响应或 401。
8. **不引入新依赖除非必要。** 本项目倾向于手写小工具（如 URL 编码）
   而不是加依赖；加依赖需在 PR/提交信息里说明理由。

## 三、分层与代码约定

```text
src/api/        接口层：一个文件一个端点
src/question/   题型层：一个文件一个题型
src/transport.rs  网络与速率控制
src/session.rs    会话与凭据
src/bin/uai/      CLI（commands.rs 是各子命令的实现）
examples/         诊断用小程序（审计、探测），不可被 lib 依赖
```

- **接口层不做业务判断**，只拼请求、解析响应；**题型层不发网络请求**。
- 注释与文档串用中文，风格是"说清为什么这么做"，而不是复述代码。
  凡是从真实响应里得出的结论，**标注"实测"并写清证据来源**。
- 新增行为尽量配一个**回归测试**，尤其是踩过的坑。
- 二进制名固定为 `uai`，**不要改**。

## 四、领域概念（最容易搞错的地方）

平台上有**三本互不相等的账**，把它们混起来是本仓库历史上最大的坑：

| 账 | 含义 | 归属 |
| --- | --- | --- |
| **必修** | 本班要求完成哪些任务 | **按班级**——同一本教材挂在不同班级时必修集可能完全不同 |
| **已完成** | 该任务是否做过 | 按**教材实例**，跨班级共享 |
| **计分点** | 该任务得分是否达标 | **按班级**，由平台考核方案计算 |

推论：**「交过」不等于「得分」**；判靶必须指定班级；客户端回读只说明请求被受理，
不代表成绩变化。新增功能前，先确认自己站在哪本账上。

## 五、验证（提交前必须全绿）

```powershell
cargo test --offline                  # 319 个测试，全离线
cargo clippy --offline --all-targets  # 0 warning
rustfmt --edition 2024 <changed files>
cargo build --release --offline
```

- **本地一律加 `--offline`**：本机 cargo 缓存齐全，离线构建是硬约定。
  ⚠️ **但 CI 上必须去掉它、改用 `--locked`**：GitHub runner 是干净环境，
  `--offline` 会直接找不到依赖而失败。见 `.github/workflows/release.yml`。
- 测试**不得**依赖真实账号、真实网络或时间敏感数据；用固定夹具。
- 改完 `README.md` / `AGENTS.md` 中的事实性描述时，同步更新测试数量等数字。

## 六、什么时候该停下来问

- 需要写入**考试 / 测验**类数据
- 需要绕过速率限制、或用多账号分摊
- 需要把真实账号数据写进代码或测试
- 平台接口行为与文档描述冲突，且无法用只读请求判定

这些情况下**不要"先实现出来再说"**：先说明现状与风险，等人类决策。
